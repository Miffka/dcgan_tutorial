from setuptools import find_packages, setup

setup(
    name='dcgan',
    packages=find_packages(),
    version='0.1.0',
    description='Repository to reproduce PyTorch tutorial on DCGAN',
    author='Miffka',
    license='',
)
